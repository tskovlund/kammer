{
  "name": "kammer-commitlint",
  "private": true,
  "description": "Dev-only tooling: commitlint for Conventional Commits enforcement (SPEC §17). Not part of the application build.",
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0"
  }
}
